# 反思写作工作表：聚焦卡壳任务

本文件只整理可核验事实和追问，不提供可直接提交的第一人称正文。建议选择 3 个最能体现判断变化的事件，避免把反思写成流水账。

| 卡壳事件 | 可核验证据 | 值得回答的问题 |
|---|---|---|
| 多次需要恢复/继续任务 | 当前会话记录；最终提交 `19d5010`–`1f65783` | 卡住时你如何判断状态是否保存？怎样的阶段性证据能降低长任务中断成本？ |
| `python`、`node` 不在 PATH | `docs/T1_TDD.md`、`T2_TDD.md`；部署构建记录 | 为什么“命令找不到”不是实现失败？你用什么证据把环境问题与代码问题分开？ |
| Windows 不能创建符号链接 | `docs/T2_TDD.md`；2 条平台条件跳过 | 跳过测试何时合理？怎样避免把 skip 偷换成“该风险已完全验证”？ |
| 同秒、同大小重写触发 Python pyc 缓存 | `docs/T5_TDD.md` | 当测试失败来自夹具而非产品代码时，TDD 是阻碍还是帮助？你如何证明根因？ |
| 测试耗时破坏重复失败指纹 | `docs/T3_TDD.md`、`tests/test_feedback.py` | 客观反馈为什么也需要归一化？哪些字段应保留，哪些属于噪声？ |
| 独立 review 发现宿主执行逃逸 | `docs/CODE_REVIEW.md`、`246e3e2` | 为什么 31+ 个测试全绿仍可能有 Critical 安全问题？白名单与 OS 沙箱的边界是什么？ |
| HITL/WebUI 首版不完整 | `docs/CODE_REVIEW.md`、`246e3e2` | “有审批类”为什么不等于用户真的能审批？功能存在与端到端可用如何区分？ |
| 部署时无 WSL、网络受限、Git ownership、token 过期 | `AGENT_LOG.md` 最终交付项 | 逐项修复比反复重跑多了哪些诊断步骤？哪一次重试是无效的，为什么？ |
| 无远端，无法产生真实 MR/PR 与远端 CI pass | `git remote -v` 为空；`CI_CD_RECORD.md` | 为什么不能用本地测试冒充远端 pass？你会怎样调整项目节奏，让 MR 在合并前产生？ |

## 请学生先写下的个人答案

每题 2–4 句即可；这些答案会成为正文最有价值的个人材料。

1. 哪个卡壳事件最让你意外？你当时第一反应是什么？
2. 哪一次你不同意或修正了智能体的判断？具体依据是什么？
3. 哪个测试最像“形式”，哪个测试真正改变了设计？
4. 如果没有独立 review，你认为最可能漏交或出事故的是什么？
5. 多次任务中断后，什么记录最帮助你继续，而哪些上下文仍然丢失？
6. 你认为 subagent 单次任务的理想颗粒度是什么？用 T2/T3 举例。
7. 如果重做，你会在哪个提交之前建立远端和 MR？

## 完成检查

- 正文为 1500–2500 字，第一人称判断多于过程复述。
- 至少引用 3 个具体卡壳事件，并说明判断如何变化。
- 明确区分真实 Superpowers 插件调用、方法参考和普通 agent 工具。
- 不声称不存在的 MR、远端 CI、Docker 本地构建或人工操作。
- 删除 `REFLECTION.md` 中所有 `[本人填写]` 和模板提示。
