# 个人反思

## 1. 从“代码能运行”到理解 harness

本项目最终选择了 A 类 Coding Agent Harness。刚开始时，我对 harness 的理解更接近“把 LLM、工具和测试串起来的执行框架”，实现过程中才逐渐认识到，真正决定它是否可靠的不是能否自动生成代码，而是能否把工作区边界、危险动作拦截、失败反馈、人工审批和停止条件写成确定性的程序机制。因此，我把治理和反馈闭环作为重点：模型可以提出动作，但动作能否执行、失败信息如何回灌、重复失败何时熔断，都不能只依赖提示词。

## 2. 最意外的卡壳与判断修正

最让我意外的是“同秒、同大小重写触发 Python pyc 缓存”。T5 测试中，业务代码看起来完全符合预期，结果却时红时绿。我的第一反应是怀疑 harness 的反馈回灌逻辑或断言有问题，于是反复重跑测试、核对动作序列，但没有找到稳定的业务缺陷。继续对比源文件修改时间、文件大小和字节码生成时间后，才发现测试夹具会在同一秒内把模块改写成大小相同的新内容，Python 因而继续使用旧字节码。最终通过让夹具确定性地使缓存失效解决了问题。这个经历改变了我的判断顺序：测试失败首先是一条证据，但它既可能指向产品代码，也可能指向夹具、缓存或运行环境，不能把“红灯”等同于“业务实现错误”。

我也两次修正了智能体给出的完成判断。T2 中，最初测试直接断言危险动作错误提示的文本，我将它改成断言稳定错误码和拦截状态。提示文案可能因语言、系统或版本变化而改变，而错误码属于 SPEC 定义的接口契约，测试应该验证行为而不是措辞。更重要的一次发生在两阶段评审：功能测试已经大面积通过，但白盒审查发现默认宿主执行仍允许 Python/Git，而 argv 白名单并不是操作系统沙箱。攻击者可以利用 `python -c` 执行任意宿主代码，绕过工作区工具的路径限制。最终实现改为只有容器标记存在时才默认允许真实执行，宿主必须显式启用不安全模式，并额外禁止 Python `-c`、stdin、任意模块以及 Git 外部工作树参数。这说明测试通过率不能替代威胁建模和安全边界审查。

## 3. TDD 的形式与实质

最像“形式”的证据是 T1、T3、T5 初始 Red 阶段的 import error。它能证明测试先于实现，却不能证明核心机制正确；如果只保留这一类 Red/Green 截图，TDD 很容易退化成流程表演。真正改变设计的是 T3 的失败指纹测试。最初直接使用完整测试输出生成指纹，但执行耗时等字段每次都会变化，导致同一种失败被识别为不同失败，熔断和重试分类随之失效。回归测试推动实现增加噪声过滤和核心特征归一化，重新定义了失败指纹的稳定契约。

Windows 符号链接测试则提醒我，skip 也必须被诚实解释。本机缺少创建符号链接权限，因此外部 symlink 场景只能条件跳过；绝对路径和 `..` 穿越仍然通过测试，但这不能被写成“所有链接风险均已验证”。最终结果是 34 项测试通过、2 项 Windows 条件跳过。对我而言，TDD 既是放大器也是约束：它能快速暴露设计问题，但前提是断言稳定、夹具可信，并且对环境未覆盖的部分保留清楚的边界说明。

## 4. SPEC、PLAN、Subagent 与中断恢复

冷启动审查证明了 SPEC/PLAN 的质量会直接限制智能体的自主程度。只给无历史 agent 首版文档时，它没有贸然实现，而是暴露了 Action JSON、Config 默认值、LLM 端口、审批状态机和 Windows 路径语义等七类歧义。这促使 SPEC 增加核心协议 v1。相比继续口头补充背景，这种冷启动更能检验文档是否真的可执行。

我认为 subagent 的理想颗粒度是一个边界清晰、可独立测试的机制单元，包含接口、最小实现和测试，且能在一次任务内闭环。T2 的工作区隔离、命令护栏和治理测试整体较合适；T3 实际同时包含失败分类、归一化和熔断，如果重做，我会进一步拆成“归一化与指纹”和“分类与熔断”，降低单次审查负担。多次任务中断后，`AGENT_LOG.md` 中的任务、commit、人工干预点，以及 `PLAN.md` 中的验证命令最有助于恢复进度；仍然容易丢失的是对话中的设计权衡、参数调整原因和未提交的临时环境状态。这说明长期任务不能依赖聊天上下文，关键判断必须及时沉淀到版本化文档。

独立 review 还发现首版 HITL 只有审批相关对象，却没有在 AgentLoop 中形成真正的暂停与续跑；WebUI 也只是原始演示摘要。修订后，无审批回调时系统进入 `awaiting_approval`，批准后使用内容绑定、单次且会过期的 token，页面则展示工作区、安全状态和事件时间线。“有接口”不等于端到端可用，这是没有独立评审最容易漏掉的部分。

## 5. 对方法论的评价与重做计划

本项目中最有实质价值的方法是 test-driven-development、subagent-driven-development 和 requesting-code-review：前者让失败指纹问题变成可复现契约，中者降低了 T2/T3 的上下文干扰，后者发现了测试未覆盖的宿主执行与 HITL 缺口。但这些名称本身不构成证据；如果没有 Red/Green 记录、独立提交和审查修订，它们也可能只是形式。本次仓库记录的是对 Superpowers 方法的参考，并未把未安装插件冒充成真实插件调用。

如果重做，我会在冷启动修订完成的 `b59c78a` 之后、T1 编码之前就建立远端仓库、基础 CI 和分支保护，使每个 worktree 从一开始都通过 PR 评审。这样既能更早暴露 Windows 权限、PATH 和依赖安装等跨环境问题，也能完整保留每个任务的远端审查记录，而不是在交付阶段集中补齐。最终我得到的主要教训是：智能体可以加速实现，但契约质量、安全边界、证据真实性和最终交付责任仍必须由人承担。

## AI 辅助声明

本文的核心观点、案例选择和原始问答由本人提供；AI 根据本人回答及仓库记录进行了事实校验、结构整理和语言润色。本人负责逐项复核内容、确认其符合实际经历，并承担最终提交责任。
