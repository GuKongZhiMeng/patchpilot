# 个人反思（学生本人填写，1500–2500 字）

> 通用要求明确规定反思必须由学生本人撰写。下面只提供证据索引和问题提纲，不是可直接提交的 AI 代写正文。请删除本提示并用自己的经历与判断完成。

1. 哪些 Superpowers 技能最有用，哪些形式大于实质？结合 `SPEC_PROCESS.md` 的冷启动缺陷。
2. TDD 是阻碍还是放大器？可引用 `docs/T1_TDD.md` 至 `T6_T7_TDD.md`，尤其是 pyc 缓存与耗时指纹两个反例。
3. subagent 能自主多久？T2/T3 的任务颗粒度为何有效或无效？
4. SPEC/PLAN 如何影响质量？具体讨论首版缺少 Action/Config/审批契约导致冷启动 agent 停止。
5. 最有效的 prompt/context 策略是什么？为何“只给 SPEC/PLAN、不补口头解释”能发现隐含假设？
6. 凭据与分发要求迫使你补想了什么？对 keyring、Docker secret、非 root 镜像和公网认证边界进行评价。
7. 若重做会改变什么？
8. 对 Superpowers 的批判：它假设任务可拆分、测试可客观化、评审成本可承受；这些假设在本项目是否成立？

AI 辅助声明（请按实际情况修改）：AI 用于代码、规格结构和文字润色；本反思的观点、案例选择和最终文字由本人完成。

