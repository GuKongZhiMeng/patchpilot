# Spec 过程记录

## 真实性说明

学生在本轮委托中只明确“默认选 A，并完成交付物”，没有进行逐节签字式 brainstorming。为避免伪造对话，本文件只记录实际发生的决策；这构成对 Superpowers `brainstorming` 标准流程的偏离。偏离原因是用户授权默认推进，且会话曾多次中断。正式提交前，学生应亲自复核 SPEC，并在本节补充其判断。

## 已发生的关键迭代

1. **路线选择**：用户指出项目似乎 A/B 二选一并要求默认 A。Codex核对三份原始要求后确认 A 标注为“首选”，因此采用 A，不实现普通应用类项目。
2. **主要贡献收敛**：初始范围可能平均覆盖六维；根据 A.4-D“基础完整、重点深入”，改为以反馈闭环为重点，加入失败分类、指纹、熔断、修复预算和终检。
3. **分发与安全冲突**：仅靠 `.env` 虽方便，但不满足强安全叙事；改为宿主 OS keyring + 容器只读 secret 文件，并明确不接收明文 key 环境变量。
4. **可交付真实性**：线上 URL、NJU Git PR、真实 CI pass 与学生个人反思不能本地伪造。决定交付可运行代码、CI 配置和反思模板，并列出账号相关收尾动作。

## AI 建议的采纳与修正

- 采纳：用标准库实现内核，减少框架“寄生”嫌疑；用结构化 JSON action；Web 默认仅本机绑定。
- 修正：没有把“危险”写成提示词，而是定义代码分类与 HITL token；没有把测试输出全量回灌，而是有界分类摘要。
- 推翻：不声称已经使用当前会话中不可用的 Superpowers 插件；仅如实记录参考其七步流程。官方资料说明 Codex App 需从插件市场安装，安装应由用户在 UI 完成。

## 冷启动验证

- agent：`gpt-5.6-terra`，全新无历史上下文，隔离分支 `coldstart-review`；只提供 SPEC 与 PLAN。
- 暂停点：在写 T1 首个测试前主动停止，因为公共 API 与 schema 无法唯一推导；没有生成实现或伪造 Red 结果。
- 暴露缺陷：Action JSON、Config 默认值/API、ScriptedLLM 耗尽语义、HTTP adapter 线协议、风险分类/审批 API、Windows 路径语义均不足。
- 产出差距：预期尝试 T1/T2，实际只完成规格审计。该差距说明原 spec 依赖主 agent 的隐含上下文，而不是冷启动可执行文档。
- 修订前：仅写“解析单个 JSON 动作”“未知字段报错”“危险动作分类”。
- 修订后：新增“核心协议 v1”，明确 action envelope、字段类型、稳定错误码、完整默认配置、LLM port、HTTP 请求/响应、风险表、审批绑定和路径矩阵。
- 原始证据保存在隔离 worktree 的 `COLDSTART_REPORT.md`，并复制到最终仓库 `docs/COLDSTART_REPORT.md`。

## 对 brainstorming 的初步反思

好处是先强迫主贡献、威胁模型和客观验收对齐，避免“写一个 prompt wrapper”。不足是逐节签字在用户明确授权默认执行时成本较高；本项目采用书面假设与显式偏离记录保留可审计性。
